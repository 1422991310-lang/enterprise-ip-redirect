{
  "name": "enterprise-country-redirect-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "test": "node scripts/test-worker.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
